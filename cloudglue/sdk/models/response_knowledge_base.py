# coding: utf-8

"""
    Cloudglue API

    API for Cloudglue

    The version of the OpenAPI document: 0.7.0
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from cloudglue.sdk.models.knowledge_base_collections import KnowledgeBaseCollections
from cloudglue.sdk.models.knowledge_base_default import KnowledgeBaseDefault
from cloudglue.sdk.models.knowledge_base_files import KnowledgeBaseFiles
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

RESPONSEKNOWLEDGEBASE_ONE_OF_SCHEMAS = ["KnowledgeBaseCollections", "KnowledgeBaseDefault", "KnowledgeBaseFiles"]

class ResponseKnowledgeBase(BaseModel):
    """
    Knowledge base configuration. Determines which content the model searches for context.  Three source modes are supported: - `collections`: Search within specific collections (default, backward-compatible) - `files`: Search specific files by URL or file ID (uses default index) - `default`: Search all default-indexed files for the account
    """
    # data type: KnowledgeBaseCollections
    oneof_schema_1_validator: Optional[KnowledgeBaseCollections] = None
    # data type: KnowledgeBaseFiles
    oneof_schema_2_validator: Optional[KnowledgeBaseFiles] = None
    # data type: KnowledgeBaseDefault
    oneof_schema_3_validator: Optional[KnowledgeBaseDefault] = None
    actual_instance: Optional[Union[KnowledgeBaseCollections, KnowledgeBaseDefault, KnowledgeBaseFiles]] = None
    one_of_schemas: Set[str] = { "KnowledgeBaseCollections", "KnowledgeBaseDefault", "KnowledgeBaseFiles" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    discriminator_value_class_map: Dict[str, str] = {
    }

    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = ResponseKnowledgeBase.model_construct()
        error_messages = []
        match = 0
        # validate data type: KnowledgeBaseCollections
        if not isinstance(v, KnowledgeBaseCollections):
            error_messages.append(f"Error! Input type `{type(v)}` is not `KnowledgeBaseCollections`")
        else:
            match += 1
        # validate data type: KnowledgeBaseFiles
        if not isinstance(v, KnowledgeBaseFiles):
            error_messages.append(f"Error! Input type `{type(v)}` is not `KnowledgeBaseFiles`")
        else:
            match += 1
        # validate data type: KnowledgeBaseDefault
        if not isinstance(v, KnowledgeBaseDefault):
            error_messages.append(f"Error! Input type `{type(v)}` is not `KnowledgeBaseDefault`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in ResponseKnowledgeBase with oneOf schemas: KnowledgeBaseCollections, KnowledgeBaseDefault, KnowledgeBaseFiles. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in ResponseKnowledgeBase with oneOf schemas: KnowledgeBaseCollections, KnowledgeBaseDefault, KnowledgeBaseFiles. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into KnowledgeBaseCollections
        try:
            instance.actual_instance = KnowledgeBaseCollections.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into KnowledgeBaseFiles
        try:
            instance.actual_instance = KnowledgeBaseFiles.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into KnowledgeBaseDefault
        try:
            instance.actual_instance = KnowledgeBaseDefault.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into ResponseKnowledgeBase with oneOf schemas: KnowledgeBaseCollections, KnowledgeBaseDefault, KnowledgeBaseFiles. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into ResponseKnowledgeBase with oneOf schemas: KnowledgeBaseCollections, KnowledgeBaseDefault, KnowledgeBaseFiles. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], KnowledgeBaseCollections, KnowledgeBaseDefault, KnowledgeBaseFiles]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())



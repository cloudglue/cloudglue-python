# coding: utf-8

"""
    Cloudglue API

    API for Cloudglue

    The version of the OpenAPI document: 0.6.7
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictBool, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from typing import Optional, Set
from typing_extensions import Self

class ShotConfig(BaseModel):
    """
    ShotConfig
    """ # noqa: E501
    detector: Optional[StrictStr] = Field(default=None, description="Detection algorithm: • **adaptive**: Designed for dynamic footage with camera movement and action (default) • **content**: Optimized for controlled footage with clear visual transitions")
    max_duration_seconds: Optional[Annotated[int, Field(le=600, strict=True, ge=1)]] = Field(default=None, description="Maximum duration for each segment in seconds (1 second to 10 minutes, default: 5 minutes)")
    min_duration_seconds: Optional[Annotated[int, Field(le=600, strict=True, ge=1)]] = Field(default=None, description="Minimum duration for each segment in seconds (1 second to 10 minutes, default: 1 second)")
    fill_gaps: Optional[StrictBool] = Field(default=None, description="When true, gaps between detected shots are filled: gaps greater than or equal to min_duration_seconds become their own segments (split by max_duration_seconds if needed), and shorter gaps are merged into the nearest adjacent segment. Defaults to true.")
    __properties: ClassVar[List[str]] = ["detector", "max_duration_seconds", "min_duration_seconds", "fill_gaps"]

    @field_validator('detector')
    def detector_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['content', 'adaptive']):
            raise ValueError("must be one of enum values ('content', 'adaptive')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of ShotConfig from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of ShotConfig from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "detector": obj.get("detector"),
            "max_duration_seconds": obj.get("max_duration_seconds"),
            "min_duration_seconds": obj.get("min_duration_seconds"),
            "fill_gaps": obj.get("fill_gaps")
        })
        return _obj



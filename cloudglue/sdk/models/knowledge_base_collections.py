# coding: utf-8

"""
    Cloudglue API

    API for Cloudglue

    The version of the OpenAPI document: 0.6.7
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional
from typing_extensions import Annotated
from cloudglue.sdk.models.entity_backed_knowledge_config import EntityBackedKnowledgeConfig
from cloudglue.sdk.models.search_filter import SearchFilter
from typing import Optional, Set
from typing_extensions import Self

class KnowledgeBaseCollections(BaseModel):
    """
    Search within specific collections. This is the default source mode and is backward-compatible — the `source` field can be omitted.
    """ # noqa: E501
    source: Optional[StrictStr] = Field(default=None, description="Source mode. Can be omitted for backward compatibility (defaults to `collections`).")
    type: Optional[StrictStr] = Field(default=None, description="The type of knowledge base interaction pattern.  - `general_question_answering` (default): Search-and-answer over media collections. - `entity_backed_knowledge`: Enables structured entity lookups alongside video content. Requires `entity_backed_knowledge_config` and `nimbus-002-preview` model.")
    collections: Annotated[List[StrictStr], Field(min_length=1)] = Field(description="Collection IDs to search for relevant context")
    filter: Optional[SearchFilter] = Field(default=None, description="Optional filter to narrow down the search within collections.  Supports filtering by `metadata`, `video_info`, and `file` properties. Each filter is an array of criteria with `path`, `operator`, and `valueText` or `valueTextArray`.  Supported operators: `Equal`, `NotEqual`, `LessThan`, `GreaterThan`, `In`, `ContainsAny`, `ContainsAll`.  Examples: - Filter by file ID: `{\"file\": [{\"path\": \"id\", \"operator\": \"Equal\", \"valueText\": \"file-uuid\"}]}` - Filter by metadata: `{\"metadata\": [{\"path\": \"speaker\", \"operator\": \"Equal\", \"valueText\": \"Amy\"}]}` - Filter by duration: `{\"video_info\": [{\"path\": \"duration_seconds\", \"operator\": \"GreaterThan\", \"valueText\": \"60\"}]}`")
    entity_backed_knowledge_config: Optional[EntityBackedKnowledgeConfig] = None
    __properties: ClassVar[List[str]] = ["source", "type", "collections", "filter", "entity_backed_knowledge_config"]

    @field_validator('source')
    def source_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['collections']):
            raise ValueError("must be one of enum values ('collections')")
        return value

    @field_validator('type')
    def type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['general_question_answering', 'entity_backed_knowledge']):
            raise ValueError("must be one of enum values ('general_question_answering', 'entity_backed_knowledge')")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of KnowledgeBaseCollections from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        """
        excluded_fields: Set[str] = set([
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of filter
        if self.filter:
            _dict['filter'] = self.filter.to_dict()
        # override the default output from pydantic by calling `to_dict()` of entity_backed_knowledge_config
        if self.entity_backed_knowledge_config:
            _dict['entity_backed_knowledge_config'] = self.entity_backed_knowledge_config.to_dict()
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of KnowledgeBaseCollections from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "source": obj.get("source"),
            "type": obj.get("type"),
            "collections": obj.get("collections"),
            "filter": SearchFilter.from_dict(obj["filter"]) if obj.get("filter") is not None else None,
            "entity_backed_knowledge_config": EntityBackedKnowledgeConfig.from_dict(obj["entity_backed_knowledge_config"]) if obj.get("entity_backed_knowledge_config") is not None else None
        })
        return _obj



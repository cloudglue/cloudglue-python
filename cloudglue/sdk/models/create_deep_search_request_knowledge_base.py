# coding: utf-8

"""
    Cloudglue API

    API for Cloudglue

    The version of the OpenAPI document: 0.6.7
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import json
import pprint
from pydantic import BaseModel, ConfigDict, Field, StrictStr, ValidationError, field_validator
from typing import Any, List, Optional
from cloudglue.sdk.models.deep_search_kb_collections import DeepSearchKBCollections
from cloudglue.sdk.models.deep_search_kb_default import DeepSearchKBDefault
from cloudglue.sdk.models.deep_search_kb_files import DeepSearchKBFiles
from pydantic import StrictStr, Field
from typing import Union, List, Set, Optional, Dict
from typing_extensions import Literal, Self

CREATEDEEPSEARCHREQUESTKNOWLEDGEBASE_ONE_OF_SCHEMAS = ["DeepSearchKBCollections", "DeepSearchKBDefault", "DeepSearchKBFiles"]

class CreateDeepSearchRequestKnowledgeBase(BaseModel):
    """
    Knowledge base configuration. Determines which content to search.  Three source modes are supported: - `collections`: Search within specific collections - `files`: Search specific files by URL or file ID (uses default index) - `default`: Search all default-indexed files for the account
    """
    # data type: DeepSearchKBCollections
    oneof_schema_1_validator: Optional[DeepSearchKBCollections] = None
    # data type: DeepSearchKBFiles
    oneof_schema_2_validator: Optional[DeepSearchKBFiles] = None
    # data type: DeepSearchKBDefault
    oneof_schema_3_validator: Optional[DeepSearchKBDefault] = None
    actual_instance: Optional[Union[DeepSearchKBCollections, DeepSearchKBDefault, DeepSearchKBFiles]] = None
    one_of_schemas: Set[str] = { "DeepSearchKBCollections", "DeepSearchKBDefault", "DeepSearchKBFiles" }

    model_config = ConfigDict(
        validate_assignment=True,
        protected_namespaces=(),
    )


    def __init__(self, *args, **kwargs) -> None:
        if args:
            if len(args) > 1:
                raise ValueError("If a position argument is used, only 1 is allowed to set `actual_instance`")
            if kwargs:
                raise ValueError("If a position argument is used, keyword arguments cannot be used.")
            super().__init__(actual_instance=args[0])
        else:
            super().__init__(**kwargs)

    @field_validator('actual_instance')
    def actual_instance_must_validate_oneof(cls, v):
        instance = CreateDeepSearchRequestKnowledgeBase.model_construct()
        error_messages = []
        match = 0
        # validate data type: DeepSearchKBCollections
        if not isinstance(v, DeepSearchKBCollections):
            error_messages.append(f"Error! Input type `{type(v)}` is not `DeepSearchKBCollections`")
        else:
            match += 1
        # validate data type: DeepSearchKBFiles
        if not isinstance(v, DeepSearchKBFiles):
            error_messages.append(f"Error! Input type `{type(v)}` is not `DeepSearchKBFiles`")
        else:
            match += 1
        # validate data type: DeepSearchKBDefault
        if not isinstance(v, DeepSearchKBDefault):
            error_messages.append(f"Error! Input type `{type(v)}` is not `DeepSearchKBDefault`")
        else:
            match += 1
        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when setting `actual_instance` in CreateDeepSearchRequestKnowledgeBase with oneOf schemas: DeepSearchKBCollections, DeepSearchKBDefault, DeepSearchKBFiles. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when setting `actual_instance` in CreateDeepSearchRequestKnowledgeBase with oneOf schemas: DeepSearchKBCollections, DeepSearchKBDefault, DeepSearchKBFiles. Details: " + ", ".join(error_messages))
        else:
            return v

    @classmethod
    def from_dict(cls, obj: Union[str, Dict[str, Any]]) -> Self:
        return cls.from_json(json.dumps(obj))

    @classmethod
    def from_json(cls, json_str: str) -> Self:
        """Returns the object represented by the json string"""
        instance = cls.model_construct()
        error_messages = []
        match = 0

        # deserialize data into DeepSearchKBCollections
        try:
            instance.actual_instance = DeepSearchKBCollections.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into DeepSearchKBFiles
        try:
            instance.actual_instance = DeepSearchKBFiles.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))
        # deserialize data into DeepSearchKBDefault
        try:
            instance.actual_instance = DeepSearchKBDefault.from_json(json_str)
            match += 1
        except (ValidationError, ValueError) as e:
            error_messages.append(str(e))

        if match > 1:
            # more than 1 match
            raise ValueError("Multiple matches found when deserializing the JSON string into CreateDeepSearchRequestKnowledgeBase with oneOf schemas: DeepSearchKBCollections, DeepSearchKBDefault, DeepSearchKBFiles. Details: " + ", ".join(error_messages))
        elif match == 0:
            # no match
            raise ValueError("No match found when deserializing the JSON string into CreateDeepSearchRequestKnowledgeBase with oneOf schemas: DeepSearchKBCollections, DeepSearchKBDefault, DeepSearchKBFiles. Details: " + ", ".join(error_messages))
        else:
            return instance

    def to_json(self) -> str:
        """Returns the JSON representation of the actual instance"""
        if self.actual_instance is None:
            return "null"

        if hasattr(self.actual_instance, "to_json") and callable(self.actual_instance.to_json):
            return self.actual_instance.to_json()
        else:
            return json.dumps(self.actual_instance)

    def to_dict(self) -> Optional[Union[Dict[str, Any], DeepSearchKBCollections, DeepSearchKBDefault, DeepSearchKBFiles]]:
        """Returns the dict representation of the actual instance"""
        if self.actual_instance is None:
            return None

        if hasattr(self.actual_instance, "to_dict") and callable(self.actual_instance.to_dict):
            return self.actual_instance.to_dict()
        else:
            # primitive type
            return self.actual_instance

    def to_str(self) -> str:
        """Returns the string representation of the actual instance"""
        return pprint.pformat(self.model_dump())


